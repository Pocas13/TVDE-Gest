# Publicação segura

Usar `PUBLICAR.ps1`. O script valida sintaxe, confirma o binding D1, aplica migrações remotas e só depois publica.

Nunca colocar `.dev.vars`, Client Secrets ou signing keys no GitHub.
