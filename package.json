{
  "name": "tvde-gest",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "check": "node --check src/worker.js && node --check src/db.js && node --check src/analytics.js && node --check src/imports.js && node --check src/portal.js && node --check src/diagnostics.js && node --check src/settlements.js && node --check src/platforms/bolt.js && node --check src/platforms/uber.js && node --check public/assets/app.js && node --check public/assets/driver.js && node scripts/check.mjs",
    "db:create": "wrangler d1 create tvde-gest-db --binding DB --update-config",
    "db:migrate:local": "wrangler d1 migrations apply DB --local",
    "db:migrate:remote": "wrangler d1 migrations apply DB --remote"
  },
  "devDependencies": {
    "wrangler": "^4.68.0"
  },
  "description": "TVDE Gest — cockpit SaaS para gestão de frotas TVDE, Bolt, Uber, histórico, acertos e área do motorista"
}
