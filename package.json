{
  "name": "tvde-gest",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "check": "node --check src/worker.js && node --check src/db.js && node --check src/settlements.js && node --check src/platforms/bolt.js && node --check src/platforms/uber.js && node scripts/check.mjs",
    "db:create": "wrangler d1 create tvde-gest-db",
    "db:migrate:local": "wrangler d1 migrations apply tvde-gest-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply tvde-gest-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.68.0"
  },
  "description": "TVDE Gest com Bolt, Uber, Cloudflare D1, sincronização automática e acertos semanais"
}
