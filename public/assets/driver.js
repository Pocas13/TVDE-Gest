const root = document.getElementById('driver-portal');
const money = (cents = 0) => new Intl.NumberFormat('pt-PT', { style: 'currency', currency: 'EUR' }).format(Number(cents || 0) / 100);
const number = (value = 0, digits = 0) => new Intl.NumberFormat('pt-PT', { maximumFractionDigits: digits }).format(Number(value || 0));
const token = new URLSearchParams(location.search).get('token');
function kpi(label, value, sub) { return `<article class="kpi"><span class="kpi-label">${label}</span><div class="kpi-value">${value}</div><div class="kpi-sub">${sub}</div></article>`; }
async function load() {
  try {
    const response = await fetch(`/api/portal/bootstrap?token=${encodeURIComponent(token || '')}&period=year`); const data = await response.json(); if (!response.ok || !data.ok) throw new Error(data.error || 'Acesso inválido.');
    const d = data.driver, s = d.total;
    root.innerHTML = `<section class="portal-hero"><p>Área privada do motorista</p><h1>${d.name}</h1><p>Dados armazenados no TVDE Gest · acesso válido até ${new Date(data.expiresAt).toLocaleDateString('pt-PT')}</p></section><div class="kpi-grid">${kpi('Ganhos disponíveis', money(s.platformPayableCents), 'Bolt + Uber no período disponível')}${kpi('Base calculada', money(s.settlementBaseCents), 'Depois de retirar o IVA e repor gorjetas/portagens')}${kpi('Viagens', number(s.trips), `${number(s.hoursOnline, 1)} horas online`)}${kpi('Média por hora', money(s.revenuePerOnlineHourCents), 'Sobre o tempo online')}${kpi('Gorjetas', money(s.tipsCents), 'Valor integral do motorista')}${kpi('Score', `${d.score}/100`, 'Indicador interno de produtividade')}</div><div class="section-grid" style="margin-top:14px"><section class="panel"><div class="panel-head"><div><h3>Plataformas</h3><p>Resumo individual</p></div></div><div class="panel-body"><div class="data-strip"><div class="mini-stat"><span>Bolt</span><strong>${money(d.bolt.platformPayableCents)}</strong></div><div class="mini-stat"><span>Uber</span><strong>${money(d.uber.platformPayableCents)}</strong></div><div class="mini-stat"><span>Portagens</span><strong>${money(s.tollsCents)}</strong></div><div class="mini-stat"><span>IVA retirado</span><strong>${money(s.vatCents)}</strong></div></div></div></section><section class="panel"><div class="panel-head"><div><h3>Privacidade</h3><p>Este acesso é individual</p></div></div><div class="panel-body"><div class="insight success"><span class="insight-icon">✓</span><div><strong>Só os teus dados</strong><p>Esta ligação não permite consultar outros motoristas, viaturas ou definições da empresa.</p></div></div></div></section></div>`;
  } catch (error) { root.innerHTML = `<div class="error-card"><div><strong>Não foi possível abrir a área do motorista.</strong><p>${error.message}</p><p>Solicita uma nova ligação ao gestor da frota.</p></div></div>`; }
}
load();
